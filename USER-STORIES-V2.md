# Stock Monitor — 用户故事分析与优化建议（V2）

> 基于 2026-05-22 最新代码（commit 584b68f）全面重审。与原始 USER-STORIES.md 独立，保留其作为历史参考。

---

## 一、核心用户故事地图

按典型投资决策流程，将功能串联为一条完整的故事线：

```
发现机会 → 分析验证 → 决策执行 → 持仓跟踪 → 风险控制
```

---

### 阶段 1：发现机会

| 编号 | 用户故事 | 对应功能 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| US-01 | 作为投资者，我想看到自选股的实时涨跌，以便快速感知市场变化 | 自选股列表 + WebSocket 实时行情 | P0 | ✅ 已实现 |
| US-02 | 作为投资者，我想输入行业关键词搜索 AI 推荐的股票，以便发现我关注领域的新机会 | 推荐发现（LLM 推荐 + 评分排序） | P1 | ✅ 已实现 |
| US-03 | 作为投资者，我想看到所有自选股的买入/卖出信号评分排名，以便优先关注信号最强的标的 | 技术指标分析 — 买卖信号评分 | P1 | ✅ 已实现 |
| US-03a | 作为投资者，我想在首页一眼看到市场指数、自选股异动和最强信号 | 仪表盘（指数卡片 + 涨跌榜 + 信号 + 预警） | P0 | ✅ 已实现 |

**当前问题**：

- AI 推荐与自选股联动仍然较弱。用户自选中某板块集体异动时，推荐引擎不会主动提示"你关注的 XX 板块今日活跃，建议关注以下相关标的"。
- 仪表盘的"最强买入信号"数据来源是 signal_history 表，需要用户主动触发过分析才会产生数据。新用户看到的是空数据。
- 仪表盘涨跌榜实时重排依赖前端 WebSocket 消息，但排序范围仅限于已加载到 quoteCache 的股票，初次加载后需要等待 WebSocket 推送才能补全。

---

### 阶段 2：分析验证

| 编号 | 用户故事 | 对应功能 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| US-04 | 作为投资者，我想看 K 线图和均线，以便判断趋势方向 | K 线图表（TradingView Lightweight Charts） | P0 | ✅ 已实现 |
| US-05 | 作为投资者，我想切换不同周期（5分/日/周/月）查看 K 线，以便从多时间维度验证判断 | 多周期切换 + 自适应 MA 参数 | P0 | ✅ 已实现 |
| US-06 | 作为投资者，我想知道具体是哪些技术指标触发了买卖信号，以便理解信号背后的逻辑 | 技术指标明细（10 买入信号 + 8 卖出信号表格） | P1 | ✅ 已实现 |
| US-07 | 作为投资者，我想让 AI 从多个策略角度分析一只股票，以便获得更全面的决策参考 | AI 策略分析（18 种策略可选） | P1 | ✅ 已实现 |
| US-08 | 作为投资者，我想看 AI 综合分析报告中的多空分歧和共识，以便了解市场对这只股票的分歧程度 | Comprehensive 综合分析（17 策略聚合 + LLM 汇总） | P1 | ✅ 已实现 |

**当前问题**：

- **K 线图与信号分析仍割裂**。K 线图上虽有基于 RSI/MACD 的简单买卖箭头标记，但信号分析组件（analysis.js）中详尽的 18 个加权信号评分并未渲染到 K 线图上。用户在分析 Tab 看到"MACD 顶背离 (2.5分)"后，切换到 K 线图只能看到通用箭头，看不到具体对应的背离标注。
- 策略分析结果是纯文本 Markdown，没有关联 K 线图上的可视化标注（如"金叉点"、"背离段"在哪个 K 线位置）。
- **仍缺少基本面数据**（PE、PB、ROE、营收增速等），AI 分析完全依赖技术面 K 线数据 + SMA/RSI/MACD 派生指标。
- 信号分析完全在客户端计算（indicators.js），与后端 AI 策略分析各走各路，两者的结论没有交叉验证。

---

### 阶段 3：决策执行

| 编号 | 用户故事 | 对应功能 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| US-09 | 作为投资者，我想记录买入的股票、价格和数量，以便后续跟踪盈亏 | 持仓管理 — 添加持仓 | P0 | ✅ 已实现 |
| US-10 | 作为投资者，我想把感兴趣的股票加入自选，以便持续跟踪 | 自选股 — 添加（支持搜索） | P0 | ✅ 已实现 |

**当前问题**：

- 持仓添加仍为纯手动录入。从 AI 推荐或信号分析到实际买入之间没有衔接——推荐卡片上有"添加到自选"按钮，但没有"加入持仓"的快捷入口。
- **没有交易笔记功能**。投资者买入时的理由、预期、止损位等信息无法记录。
- 推荐发现中的股票详情弹窗已非常完整（信号表格 + AI 策略分析），但缺少"一键加入持仓"按钮。

---

### 阶段 4：持仓跟踪

| 编号 | 用户故事 | 对应功能 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| US-11 | 作为投资者，我想看每笔持仓的实时盈亏，以便了解当前收益状况 | 持仓列表 + WebSocket 实时更新 | P0 | ✅ 已实现 |
| US-12 | 作为投资者，我想看所有持仓的总成本和总市值，以便把握整体仓位 | 持仓汇总栏（总投入/总市值/总盈亏/总盈亏率） | P0 | ✅ 已实现 |
| US-13 | 作为投资者，我想更新持仓的股数或成本（如加仓/减仓），以便保持数据准确 | 持仓编辑 | P1 | ✅ 已实现 |

**当前问题**：

- 持仓管理只有基础 CRUD。**没有分批买入/卖出记录**，无法追踪同一只股票的多笔交易历史。当前 `holdings` 表的复合主键是 `(symbol, user_id)`，意味着一只股票只能有一条记录。
- **没有收益曲线或历史收益记录**。盈亏数字只在当前价格下有意义，无法回顾"上个月赚了多少"。
- **没有持仓集中度分析**（行业分布、市场分布），用户可能无意识地过度集中仓位。
- 持仓更新方法（`HoldingRepo.Update`）采用读-改-写模式，不在事务内，理论上存在并发覆盖风险。

---

### 阶段 5：风险控制

| 编号 | 用户故事 | 对应功能 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| US-14 | 作为投资者，我想设置价格预警，以便在价格达到目标时及时知道 | 价格预警（涨破/跌破/涨跌幅达%） | P1 | ✅ 已实现 |
| US-15 | 作为投资者，我想看到信号评分突破关键阈值时的提醒，以便关注极端信号 | 信号阈值告警（WebSocket 实时推送） | P2 | ✅ 已实现 |

**当前问题**：

- 预警类型偏少（仅 above/below/change_pct 三种）。缺少**移动止盈**（价格从最高点回落 X% 时触发）、**成交量条件**（成交量突破 N 日均量 X 倍）。
- 预警触发后的消息较为简单（如"涨破 100.00"），没有关联持仓上下文（如"当前持仓该股 1000 股，浮盈 15%，触发止盈线"）。
- 信号阈值告警的对比基准是 7 天前的记录，但 7 天这个窗口是硬编码的，不可配置。
- 预警日志接口 `GET /api/alerts/logs` 不按 user_id 过滤，存在跨用户数据泄露风险。

---

## 二、用户故事缺口 — 缺失的关键场景

| 编号 | 用户故事 | 重要性 | 状态 |
|------|---------|--------|------|
| US-16 | 作为投资者，我想回顾自己的历史交易和决策，以便总结经验教训 | 高 | ❌ 未实现 |
| US-17 | 作为投资者，我想知道某只股票在我加入自选后的涨跌幅，以便评估自己的选股能力 | 中 | ❌ 未实现 |
| US-18 | 作为投资者，我想对比两只股票的走势和指标，以便做二选一的决策 | 中 | ❌ 未实现 |
| US-19 | 作为投资者，我想导出持仓或分析数据，以便在其他工具中使用 | 低 | ❌ 未实现 |
| US-20 | 作为投资者，我想收到每日/每周市场总结推送，以便保持对市场的感知 | 低 | ❌ 未实现 |

---

## 三、优化建议

### 已完成（原始文档中的 P0/P1 建议已实现）

1. **首页仪表盘** — DashboardComponent + `/api/dashboard` 端点，包含指数卡片、涨跌 Top 3、最强买入信号、最新预警
2. **股票搜索** — 通过东方财富 suggest API 搜索，支持 A 股/港股/美股
3. **买入信号评分排名** — analysis.js 提供 10 买入信号 + 8 卖出信号的加权评分，按分数排序
4. **AI 策略分析** — 18 种策略 + 综合分析，集成在自选股详情和推荐弹窗中
5. **信号阈值告警** — 买入/卖出百分比穿越阈值时通过 WebSocket 实时推送

---

### 优先级 P0 — 影响核心体验，建议尽快做

**1. K 线图与信号分析联动**

目前 K 线图上仅有基于 RSI/MACD 的简单通用箭头，与 analysis.js 中 18 个加权信号完全割裂。具体改进：

- 将信号分析中触发的关键信号（如"MACD 顶背离 @ 2026-05-20"、"MA5 金叉 MA20 @ 2026-05-18"）以标记点形式渲染在 K 线图对应位置，点击可查看详情。
- 在分析 Tab 中点击某只股票时，直接内嵌 K 线图组件（而非要求用户手动切到 K 线 Tab 再选股票）。
- 使用 K 线图组件的 marker API 标注信号位置。

**2. 修复预警日志跨用户泄露**

`GET /api/alerts/logs` 当前调用 `alertRepo.GetLogs(limit)`，该方法无条件查询所有用户的日志。Dashboard 处理函数中虽然用 watchlist nameMap 填充了名称，但没有过滤非当前用户的预警。应改为 `GetLogsByUser(userID, limit)`，在 SQL 层加 `WHERE alerts.user_id = ?` 的 JOIN 条件。

---

### 优先级 P1 — 提升分析深度，建议纳入近期规划

**3. 交易笔记 + 推荐到持仓一键流转**

- 在添加持仓时，增加可选的"买入理由"和"目标价位"字段。这需要扩展 `holdings` 表。
- 在推荐弹窗中增加"一键加入持仓"按钮，复用已有的搜索和信号数据。
- 在信号分析详情中增加"加入持仓"入口，自动带入当前价格和信号评分摘要作为参考。

**4. 持仓收益历史**

- 新增 `portfolio_snapshots` 表，每日定时记录总市值、总成本、总盈亏。
- 前端增加简单的收益曲线图（可用 Chart.js 轻量实现）。
- 扩展到单只持仓的收益曲线（需要先实现分批交易记录）。

**5. 基本面数据补充**

东方财富 API 已在使用（搜索和行情回退），可以额外获取：

- 个股基本面：PE（市盈率）、PB（市净率）、ROE、营收增速（东方财富 `qt/stock/get` 或类似接口）
- 行业分类信息（用于持仓集中度分析）

这些数据可作为 AI 策略分析的额外输入维度，让分析结论从纯技术面升级为技术面+基本面。

**6. 分批交易记录**（数据库模型重构）

当前 `holdings` 表主键是 `(symbol, user_id)`，无法记录同一股票的多笔交易。建议：

```
holdings (symbol, user_id) → 保留为聚合视图（加权成本、总股数）
trades (id, symbol, user_id, type, shares, price, date, note) → 新增交易表
```

每次通过 API 添加/修改持仓时，自动生成一条 trade 记录。卖出的部分自动结算已实现盈亏。

**7. AI 推荐与自选股联动**

- 当自选股中某个市场/板块的股票整体上涨 >2% 时，推荐引擎提示"你关注的 HK 板块今日活跃"。
- 推荐结果中标记"你已持有 XX，同类标的还有…"（基于名称/行业相似度）。

---

### 优先级 P2 — 锦上添花，有资源时考虑

**8. 预警规则增强**

- 移动止盈：记录持仓期间的阶段性最高价，当价格从最高点回落 X% 时触发。
- 成交量条件：`type: "volume_breakout"`，当日成交量突破 N 日均量 X 倍。
- 关联上下文：触发时查询当前用户是否持仓该股，附带浮亏/浮盈信息。

**9. 股票对比功能**

选择 2-3 只股票并排展示 K 线图和关键指标。技术上可复用 KlineComponent 创建多个实例，或使用 TradingView 的多窗口模式。

**10. 自选股表现统计**

在每只自选股上显示"加入自选以来涨跌幅"。需要在 watchlist 表或新增表中记录加入时的价格快照（或从 signal_history 推算）。

**11. 盘后摘要**

每日收盘后自动生成一份简报：今日自选股涨跌概况、触发预警汇总、最强信号变化。可做成仪表盘上的一个可折叠卡片区域，或通过 WebSocket 推送通知。

---

## 四、技术债务（新增于 V2）

以下问题影响系统健壮性，建议随日常迭代逐步修复：

| 编号 | 问题 | 影响 | 建议 |
|------|------|------|------|
| TD-01 | `HoldingRepo.Update` 非事务性读-改-写 | 并发更新可能丢失数据 | 使用 `BEGIN IMMEDIATE; SELECT; UPDATE; COMMIT` |
| TD-02 | 预警日志不按 user_id 过滤 | 跨用户数据泄露风险 | 增加 `user_id` 字段或 JOIN alerts 过滤 |
| TD-03 | `watchlist.onChanged` 回调传入 nil | 新加自选后需等 30 秒 sync 周期才能开始拉行情 | 传入实际回调，在添加/删除时立即更新 emClient tracked codes |
| TD-04 | 信号分析纯前端计算，不持久化结果 | 每次打开页面都需重新拉 K 线数据计算 | 将分析结果缓存或持久化，避免重复计算 |
| TD-05 | Dashboard 涨跌榜依赖前端 WS 消息累积 | 初次加载时排序不完整 | 后端 `/api/dashboard` 已返回初始排序，前端 reRankMovers 应以此为种子初始化 quoteCache |
| TD-06 | 大盘指数硬编码（仅 4 个） | 用户无法自定义关注的市场指数 | 将指数列表改为可配置（环境变量或配置文件） |

---

## 五、优先级矩阵

```
                    高影响
                      │
     ┌────────────────┼────────────────┐
     │  K线信号联动   │  交易笔记      │
     │  预警日志修复  │  收益历史      │
     │                │  基本面数据    │
     │                │  分批交易      │
     │                │  推荐-自选联动 │
     │                │                │
低投入 ───────────────┼────────────────── 高投入
     │                │                │
     │  自选表现统计  │  预警增强      │
     │  指数可配置    │  股票对比      │
     │  onChanged修复 │  盘后摘要      │
     │                │                │
     └────────────────┼────────────────┘
                      │
                    低影响
```

**建议路线**：先做 K 线信号联动 + 预警日志修复（低投入高影响），再做交易笔记 + 分批交易（高影响但需一定投入），其余按资源排期。

---

## 六、与原始 USER-STORIES.md 的主要差异

| 维度 | 原始文档 | V2 |
|------|---------|-----|
| 已实现故事 | 仅标记 P0/P1/P2 优先级 | 增加 ✅/❌ 状态列，明确展示已实现 vs 未实现 |
| 仪表盘 | 标记为缺失，建议优先做 | 已实现，分析新出现的体验问题 |
| 新发现功能 | — | 搜索、信号告警、推荐评分、综合分析等新功能已纳入故事地图 |
| 技术债务 | 无 | 新增 6 项 TD，涵盖数据安全、并发正确性、前端初始化等 |
| 建议调整 | — | 原 P0 仪表盘建议已移除（已实现），K 线联动提升为最优先；新增预警日志修复为 P0 安全项 |
